---
title: "API Overview"
description: "Base URL, authentication, and error codes"
---

## Base URL

```
https://openapi.thunderbit.com/openapi/v1
```

## Authentication

All endpoints use HTTP Bearer (API Key):

```
Authorization: Bearer YOUR_API_KEY
```

## Error Codes

| Code | Description |
| ---- | ----------- |
| `INVALID_URL` | Invalid URL format |
| `INVALID_PARAMETER` | Invalid request parameter |
| `URL_NOT_ACCESSIBLE` | Unable to access target URL |
| `TIMEOUT` | Request timeout |
| `INSUFFICIENT_CREDITS` | Not enough credits |
| `RATE_LIMIT_EXCEEDED` | Rate limit triggered |
| `INVALID_SCHEMA` | Invalid Schema format |
| `SCHEMA_OR_PROMPT_REQUIRED` | Schema is required for extraction |
| `EXTRACTION_FAILED` | AI extraction failed |
| `DISTILL_FAILED` | Web distillation failed |
| `BATCH_SIZE_EXCEEDED` | Batch request count exceeded limit |
| `INVALID_WEBHOOK_URL` | Invalid Webhook URL format or not HTTPS |
| `WEBHOOK_DELIVERY_FAILED` | Webhook callback delivery failed |
| `JOB_NOT_FOUND` | Batch job not found |
| `INVALID_API_KEY` | API key is invalid |
| `API_KEY_DISABLED` | API key has been disabled |
| `INTERNAL_ERROR` | Internal server error |
